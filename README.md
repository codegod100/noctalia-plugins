# Noctalia Plugins

Personal Noctalia v5 plugins for `codegod100`.

This repository is structured the way Noctalia's plugin catalog expects:

- `catalog.toml` at the repo root
- one directory per plugin
- each plugin directory named after the part of its id after the `/`

Current plugins:

- `focus-toggle`: a non-widget control-center shortcut plugin
